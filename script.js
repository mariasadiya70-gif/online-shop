const products=[{id:1,name:'Classic Hoodie',cat:'Fashion',price:1290,old:1590,emoji:'👕'},{id:2,name:'Smart Watch',cat:'Electronics',price:2490,old:2990,emoji:'⌚'},{id:3,name:'Wireless Headphones',cat:'Electronics',price:1890,old:2290,emoji:'🎧'},{id:4,name:'Beauty Essentials',cat:'Beauty',price:990,old:1250,emoji:'💄'},{id:5,name:'Modern Lamp',cat:'Home',price:1490,old:1790,emoji:'💡'},{id:6,name:'Canvas Sneakers',cat:'Fashion',price:1750,old:2100,emoji:'👟'},{id:7,name:'Skincare Set',cat:'Beauty',price:1190,old:1490,emoji:'🧴'},{id:8,name:'Coffee Maker',cat:'Home',price:2990,old:3490,emoji:'☕'}];let cart=JSON.parse(localStorage.getItem('shopCart')||'[]');let category='All';const grid=document.getElementById('productsGrid');function money(n){return '৳'+n.toLocaleString('en-BD')}function render(list=products){grid.innerHTML=list.map(p=>`<article class="card"><div class="pic">${p.emoji}</div><div class="card-content"><span class="tag">${p.cat}</span><h3>${p.name}</h3><div><span class="price">${money(p.price)}</span><span class="old">${money(p.old)}</span></div><button class="add" onclick="addToCart(${p.id})">Add to Cart 🛒</button></div></article>`).join('')}function addToCart(id){const item=cart.find(x=>x.id===id);item?item.qty++:cart.push({id,qty:1});save();toast('Added to cart ✓')}function save(){localStorage.setItem('shopCart',JSON.stringify(cart));renderCart()}function renderCart(){document.getElementById('count').textContent=cart.reduce((s,x)=>s+x.qty,0);const box=document.getElementById('items');if(!cart.length){box.innerHTML='<p>Your cart is empty.</p>';document.getElementById('total').textContent='৳0';return}let total=0;box.innerHTML=cart.map(x=>{const p=products.find(p=>p.id===x.id);total+=p.price*x.qty;return `<div class="cart-item"><span class="mini">${p.emoji}</span><div style="flex:1"><b>${p.name}</b><div>${money(p.price)}</div><div class="qty"><button onclick="changeQty(${p.id},-1)">−</button> ${x.qty} <button onclick="changeQty(${p.id},1)">+</button></div></div><button onclick="removeItem(${p.id})">🗑️</button></div>`}).join('');document.getElementById('total').textContent=money(total)}function changeQty(id,n){const x=cart.find(x=>x.id===id);if(x){x.qty+=n;if(x.qty<=0)cart=cart.filter(i=>i.id!==id);save()}}function removeItem(id){cart=cart.filter(x=>x.id!==id);save()}function toast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}document.querySelectorAll('.categories button').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.categories button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');category=btn.dataset.cat;render(category==='All'?products:products.filter(p=>p.cat===category))});function search(){const q=document.getElementById('search').value.toLowerCase();render(products.filter(p=>(category==='All'||p.cat===category)&&p.name.toLowerCase().includes(q)))}document.getElementById('search').addEventListener('input',search);document.getElementById('searchBtn').onclick=search;const cartEl=document.getElementById('cart'),overlay=document.getElementById('overlay');function openCart(){cartEl.classList.add('open');overlay.classList.add('show')}function closeCart(){cartEl.classList.remove('open');overlay.classList.remove('show')}document.getElementById('cartBtn').onclick=openCart;document.getElementById('close').onclick=closeCart;overlay.onclick=closeCart;document.getElementById('theme').onclick=()=>{document.body.classList.toggle('dark');localStorage.setItem('dark',document.body.classList.contains('dark'))};if(localStorage.getItem('dark')==='true')document.body.classList.add('dark');document.getElementById('checkout').onclick=()=>{if(!cart.length)return toast('Your cart is empty');toast('Checkout coming soon 🚀')};render(products);renderCart();